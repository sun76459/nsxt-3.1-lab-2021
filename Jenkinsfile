pipeline {
    agent any
    stages {
        stage("init") {
            echo 'init...'
        }

        stage("plan") {
            echo 'plan...'
        }

        stage("apply") {
            echo 'apply...'
        }

        stage("show") {
            echo 'show...'
        }
    }
}