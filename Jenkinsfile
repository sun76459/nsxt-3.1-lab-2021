pipeline {
    agent any
    stages {
        stage("init") {
            steps {
                echo 'init...'
            }
        }

        stage("plan") {
            steps {
                echo 'plan...'
            }
        }

        stage("apply") {
            steps {
                echo 'apply...'
            }
        }

        stage("show") {
            steps {
                echo 'show...'
            }
        }
    }
}
